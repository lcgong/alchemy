/*jshint esversion: 6 */

import { snapshot } from "./snapshot";
import { fromJS, DObject, DList, keys } from "./dobject";

export default { fromJS, DObject, DList, snapshot, keys };
export { fromJS, DObject, DList, snapshot, keys };
